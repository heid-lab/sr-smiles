name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Poetry
        run: pip install "poetry>=1.8.0"

      - name: Install dependencies
        run: poetry install --with dev --extras "rxnmapper"

  lint:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Poetry
        run: pip install "poetry>=1.8.0"

      - name: Install dependencies
        run: poetry install --with dev

      - name: Run pre-commit
        run: poetry run pre-commit run --all-files --show-diff-on-failure

  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
        rdkit-version: ["2025.09.3", "latest"]

    needs: setup
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        run: pip install "poetry>=1.8.0"

      - name: Install RDKit
        run: |
          if [ "${{ matrix.rdkit-version }}" = "latest" ]; then
            conda install -c conda-forge python=${{ matrix.python-version }} rdkit -y
          else
            conda install -c conda-forge python=${{ matrix.python-version }} rdkit=${{ matrix.rdkit-version }} -y
          fi

      - name: Install dependencies
        run: poetry install --with dev --extras "rxnmapper"

      - name: Run pytest
        run: poetry run pytest --cov=src/sr_smiles --cov-report=term-missing --cov-report=xml

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
